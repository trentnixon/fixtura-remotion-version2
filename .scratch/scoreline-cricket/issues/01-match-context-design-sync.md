# 01 — Match-context mode tokens on design prototypes

**What to build:** Match-context and roster-context strips stay readable in all four Fixtura modes on club-gradient backdrops in the design preview, matching Remotion behaviour.

**Blocked by:** None — can start immediately.

**Status:** completed

- [x] Context copy uses `--match-context-text` / `--match-context-text-muted` in polish CSS
- [x] Design `scoreline-polish.css` synced with Remotion for context strip rules
- [x] Roster context strip uses mode overlay tokens

**Completion Summary:** Design polish CSS now consumes mode-aware match-context variables; roster context updated on design and Remotion sides.

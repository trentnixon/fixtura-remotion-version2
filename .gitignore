node_modules
# `dist/` holds `npm run build:package` output for GitHub installs (Strategy A).
# Remotion bundle uses other output dirs; keep `dist/` committed when releasing the package.
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out
build
build/
build/*
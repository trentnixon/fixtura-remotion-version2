# Episode — 2026-03-25

## Context

- User wanted memory logged after adding a static design LLM brief for the cricket **performances** composition (parallel to the existing Top 5 brief).

## Work Done

- Created `src/compositions/cricket/performances/.docs/llm-brief/llm-brief-cricket-performances.md`: audience, goal, artboard, content model (variable N rows, batting/bowling), typical row layout, design guidance (lead row, sponsors, safe area, multi-page note), data field checklist aligned with `performances/_types/types.ts`, sample payloads `Cricket_BattingPerformances.json` / `Cricket_BowlingPerformances.json`.
- **Broadcast Pro upcoming glass:** Added `broadcastProGlassOpacity: 'sm' | 'md' | 'lg'` with `BROADCAST_PRO_TRANSPARENT_BY_PRESET` and `resolveBroadcastProTransparentLayers()` (explicit `broadcastProTransparentLayers` overrides preset). Template default **`md`** for stronger main-row/header contrast vs stitch-like **`sm`**; **`lg`** strongest. Wired `game-card-broadcastPro` + `ThemeContextTypes`; docs under `broadcastPro/.docs/readMe.md`.

## Decisions

- None recorded.

## Results

- Performances composition now has a dedicated LLM/design brief under `.docs/llm-brief/`.
- Upcoming Broadcast Pro cards can tune glass strength via theme preset without hand-editing every alpha.

## Next

- [ ] Optional: add club/association variant briefs if other cricket compositions use that split.
- [ ] Optional: tune `md`/`lg` alpha values after preview on real gradients.

## Links

- `src/compositions/cricket/performances/.docs/llm-brief/llm-brief-cricket-performances.md`
- `src/templates/types/TemplateThemeConfig.ts` (Broadcast Pro transparent presets)
- `src/templates/variants/broadcastPro/theme.ts` / `theme/index.ts` (if refactored)

# ResultStatement Cleanup Summary

Refactored ResultStatement components by consolidating all shared types (`ResultStatementShortProps`, `ResultStatementTextProps`, `ResultStatementClassicProps`) into `_types/ResultStatementProps.ts` and utility functions (`swapResultWord`, `buildResultStatementText`) into `_utils/helpers.ts` (both `_types` and `_utils` folders are in the current ResultStatement directory - create them if they don't exist). Updated `ResultStatementClassic.tsx` to use the shared type from `_types`, removing the duplicate inline interface definition. All three component files (`ResultStatementShort.tsx`, `ResultStatementText.tsx`, `ResultStatementClassic.tsx`) now use shared types and utilities, ensuring consistency and maintainability.

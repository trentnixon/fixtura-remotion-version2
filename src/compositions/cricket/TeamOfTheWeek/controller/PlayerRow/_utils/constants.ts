/**
 * Default icon pack configuration for player row variants
 * Can be overridden per variant if needed
 */
export const DEFAULT_ICON_PACK = "icon1";

/**
 * Animation delay offsets for stat displays
 */
export const STAT_DISPLAY_DELAY_OFFSET = 20;
export const BOWLING_STAT_DELAY_OFFSET = 30;
export const STAT_SUFFIX_DELAY_OFFSET = 10;
export const PLAYER_NAME_DELAY_OFFSET = 2;

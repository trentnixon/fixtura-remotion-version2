/**
 * Default className for player list
 */
export const DEFAULT_PLAYER_LIST_CLASSNAME = "text-left font-bold";

/**
 * Default gap spacing for player list
 */
export const DEFAULT_PLAYER_LIST_GAP = "gap-2";

/**
 * Maximum length for player name truncation
 */
export const MAX_PLAYER_NAME_LENGTH = 30;

/**
 * Logo size for sponsor logos in Opposition component
 */
export const SPONSOR_LOGO_SIZE = 220;

/**
 * Default animation delay offset for sponsor logos
 */
export const SPONSOR_LOGO_DELAY_OFFSET = 5;

/**
 * Default animation delay for team header components
 */
export const DEFAULT_TEAM_HEADER_ANIMATION_DELAY = 0;

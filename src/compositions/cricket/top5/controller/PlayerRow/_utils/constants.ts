/**
 * Stagger delay multiplier for player row animations
 * Each row is delayed by index * STAGGER_DELAY_MULTIPLIER frames
 */
export const STAGGER_DELAY_MULTIPLIER = 5;

/**
 * Default duration in frames for main sequence if not specified
 */
export const DEFAULT_MAIN_DURATION = 30;

/**
 * Offset in frames before the end of the sequence for exit animation
 */
export const EXIT_ANIMATION_OFFSET = 30;

/**
 * Default restrictions for player name and team name lengths
 */
export const DEFAULT_NAME_LENGTH_RESTRICTION = 20;
export const DEFAULT_TEAM_LENGTH_RESTRICTION = 35;

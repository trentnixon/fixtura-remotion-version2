/**
 * Default logo size for player rows
 */
export const DEFAULT_LOGO_SIZE = 24;

/**
 * Small logo size for player rows (used in grid layouts)
 */
export const SMALL_LOGO_SIZE = 20;

/**
 * Default maximum length for player name truncation
 */
export const DEFAULT_NAME_LENGTH = 20;

/**
 * Default maximum length for team name truncation
 */
export const DEFAULT_TEAM_LENGTH = 35;

/**
 * Animation delay offsets for player row elements
 */
export const PLAYER_NAME_DELAY_OFFSET = 2;
export const TEAM_NAME_DELAY_OFFSET = 4;
export const MAIN_SCORE_DELAY_OFFSET = 6;
export const SCORE_SUFFIX_DELAY_OFFSET = 7;
export const LOGO_DELAY_OFFSET = 20;
export const STAT_DELAY_OFFSET = 20;
export const STAT_SUFFIX_DELAY_OFFSET = 30;

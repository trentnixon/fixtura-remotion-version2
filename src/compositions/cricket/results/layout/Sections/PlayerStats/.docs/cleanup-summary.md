# PlayerStats Cleanup Summary

Refactored all PlayerStats components by extracting shared types (`PlayerStatsProps`, `PlayerStatsClubOnlyProps`, `PlayerStatsSingleTeamProps`, `StatItemProps`, `StatSectionProps`, `TeamStatsProps`, `PlayerStat`) into `_types/PlayerStatsProps.ts` and utility functions (`computePartialTwoDayVisibility`, `isTwoDaySingleInnings`, `applyAccountClubOverlay`, `computePlayerVisibility`) into `_utils/visibility.ts`, plus helper function (`truncateText`) into `_utils/helpers.ts` (both `_types` and `_utils` folders are in the current PlayerStats directory - create them if they don't exist). Updated all component imports to use the new folder structure, removing the old `utils.ts` file from the root directory and eliminating duplicate type definitions across 7 component files.

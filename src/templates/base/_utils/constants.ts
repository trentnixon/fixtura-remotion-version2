/**
 * Z-index for the main content layer (above background and audio)
 */
export const CONTENT_Z_INDEX = 1000;

/**
 * Default duration in frames for timing sequences if not specified
 */
export const DEFAULT_SEQUENCE_DURATION = 30;

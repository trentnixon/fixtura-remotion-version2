import { baseTheme } from "../../base/theme";

/**
 * Basic template theme - extends base theme
 */
export const basicTheme = {
  ...baseTheme,
  // Add basic-specific theme properties
  gradientDegree: "0deg",
};
